package com.danielleguizamon.appmessages.di

import org.koin.dsl.module

val viewModelModule = module {
//    viewModel { MovieViewModel(get()) }
//      viewModel { CarMoviesViewModel(get()) }
//    viewModel { MovieDetailsViewModel(get()) }
}